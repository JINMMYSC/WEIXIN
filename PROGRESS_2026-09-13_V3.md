# 2026-09-13 V3 多路线推进记录

这一轮实际同时推进了逆向、Core、UI、持久化、Hamster 接线、局域网传送六条线。

## 已完成并验证

- **15 个键盘 INI 继承链已真正展开**：26 拼音、26 英文、五笔、内嵌手写、9 拼音、笔画、9 键数字、26 来源数字、9 键内嵌手写、中/英符号、Full Symbol、手写、Numpad。
- **style.ini 解析器修正**：原文件有 `[STYLE_xxx] ; comment` 形式；旧解析器会把这些节误并入上一节。本轮修复后有效 style 从错误的 17 个恢复为 **26 个**，并生成继承展开后的 `GeneratedStylesV3.swift`。
- **键位行为层**：新增 tap / 上滑 / 下滑 / 长按解析；处理中英数组值、`FLOATLIST`、Shift、语言键与返回原中文布局。
- **候选交互**：候选点击不再直接插文本；新增展开候选、长按动作入口、固定/删除兼容层，以及显示索引到 Rime 原始索引映射。
- **持久化**：剪贴板、Emoji 最近、常用语、候选固定/屏蔽均有 JSON 持久层，可放进 App Group。
- **iOS Overlay**：主键盘、候选展开、长按浮窗、工具栏、Emoji 分类、剪贴板卡片、常用语、手写识别回调、语音、翻译、AI、纠错面板均已有 SwiftUI 代码。
- **Hamster 接线**：新增 closure-based `WTClosureIMEEngine`、`WTKeyboardOverlayBinding` 和 adapter template，不绑死某个 Hamster API 版本。
- **隔空传送**：新增基于 Bonjour + Network.framework 的首版设备发现、发送、接收实现。
- **二进制行为地图**：从 `__objc_classname` / `__objc_methname` 生成分类报告；2394 个 class-name entries、30831 个 method/property strings。

## 高置信逆向行为证据

二进制中已确认存在：

- `floatingBecomeUpSwipe:` / `floatingBecomeDownSwipe:` / `floatingBecomeLongPress:`
- `_showLongPressPopupItems:sourceRect:defaultIndex:style:`
- `candidateView:didLongPressCandidate:...`
- `candidateView:didSelectCandidate:...`
- `candidateView:requireMoreCandidate:`
- `processHandWriteInput:session:`
- `selectPasteboardHistoryItem:selectedTag:`
- `getRecentlyEmojis`
- `didTapEmojiInEmojiKeyboard:skinEmoji:`
- `showCorrectionDetailViewWithCorrection:animated:isFromOpenPlus:`
- `switchPanelView:`

## 本环境验证

- `swift test`: **21 / 21 通过**。
- `swiftc -frontend -parse iOSOverlay/*.swift HamsterBridge/*.swift iOSServices/*.swift`: **PASS**。
- 真实 iOS SDK 类型检查、签名、Keyboard Extension 安装、触控/动画手感仍必须到 macOS + Xcode 做。

## 下一批

1. 用具体 Hamster 开源版本把 Rime 五个 closure 接成真正 adapter。
2. 根据 3.5.3 二进制继续细拆 candidate 固定/删除、滑删、下滑撤销、空格双击句号、长按空格语音的状态条件。
3. 手写识别器接本地实现，候选回灌统一候选栏。
4. 语音主 App/Extension IPC + Live Activity 通路。
5. Main App 设置、Share Extension、Widget/Control targets。
6. 真机逐像素与触控时序校准。
