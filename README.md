# WEIXIN — WeTypeReplicaOverlay V14

This private repository stores the user-supplied V14 build baseline as WeTypeReplicaOverlay_v14-runtime.zip. GitHub Actions expands the archive, runs Core tests and XcodeGen, compiles the Host App plus Keyboard, Share, Widget, and Voice Activity extensions, then packages an unsigned IPA and uploads complete logs.
