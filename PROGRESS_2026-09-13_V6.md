# V6 progress

V6 advances several hard steps together instead of sequentially: formerly unknown surfaces are classified, Finder/BookVideo gets a clean-room rich-content panel, Hamster/Rime gets a typed session adapter, Xcode target/entitlement/plist scaffolding is included, local handwriting/correction providers are added, and visual/motion calibration parameters are centralized for iPhone side-by-side matching.

Validation: 30/30 Core tests pass; all Swift files parse; integration/plist checks pass. The next blocking milestone is a real macOS/Xcode/iPhone build and calibration pass.
