# 微信输入法 3.5.3 原版采样表

用途：为 JINMMYSC/WEIXIN 重建项目建立离线输入基准。当前仅准备采样说明，以下结果均需要在原版上实际观察，不是预设答案。

## 先记录环境

- iPhone 型号：
- iOS 版本：
- 微信输入法显示版本：
- 当前键盘布局、主题、字体大小：
- 模糊拼音、简拼、自动纠错、联想相关设置：
- 已下载词库与模型版本（界面可见时记录）：
- 采样用的输入应用与输入框：
- 屏幕方向与显示缩放：
- 原版学习记录：已有个人学习 / 可确认清洁状态 / 无法确认。

不要为了采样卸载应用或清空学习记录。避免在含个人信息的笔记中采样。先用新建空白笔记，记录原版截图中的版本和设置，再切换到离线环境确认键盘可工作。每个用例开始清空测试文本，但不要把清空文本当作清空学习状态。

## 第一批 12 个场景

| 编号 | 操作 | 需要记录 |
|---|---|---|
| S01 | 依次输入 nihao，等待候选稳定，选择第一候选 | 每步组合串、候选及最终上屏 |
| S02 | 输入 zhongguo，选择第二候选（若存在） | 完整顺序与选非首项结果 |
| S03 | 输入 shurufa，展开候选，再收起 | 展开前后顺序、布局和交互 |
| S04 | 分别输入 xian 与 xi'an（若原版支持分隔符） | 两者候选差异；不支持也记录 |
| S05 | 输入 jintiantianqihenhao，选择第一候选 | 长句组合串、候选、上屏 |
| S06 | 输入 nh，观察简拼行为 | 相关设置与实际结果 |
| S07 | 输入 nihao，按一次删除 | 删除前后组合串与候选 |
| S08 | 输入 zhongguo，长按删除约两秒 | 全程录像、组合串与已提交文本变化 |
| S09 | 先上屏一段测试文字，再将光标移到中间并输入 ni | 光标、插入位置与上下文候选 |
| S10 | 输入 nihao，选择第三候选（若存在），再输入 nihao | 选择和学习后的顺序，缺少第三项则记录 |
| S11 | 输入拼音、切到英文输入 hello，再返回拼音 | 切换时组合串是否提交、最终文本 |
| S12 | 输入 ni，切换系统键盘再返回 | 会话是否保留、候选与界面状态 |

每例至少运行两次，保持运行顺序并记录是否发生学习变化。S10 会影响后续候选，不将前后两次差异自动视为错误。不要自动调整设置来让候选符合预期。

## 每例结果记录格式

- 编号与运行次数：
- 时间、初始文本、光标位置：
- 按键或操作序列：
- 每步组合串：
- 首屏全部候选（按实际顺序）：
- 前十候选（可观察时记录；看不到就注明）：
- 选择的位置与文字：
- 最终上屏文本：
- 截图或录像文件名：
- 是否稳定重复：
- 特殊情况：

录像保留按键到结果的过程；截图用于稳定界面对照。录像时延不是准确的引擎计算耗时，性能测试另行设计。发送文件时请附上用例编号，未执行的场景不要填写猜测结果。

## 仓库接入状态

指定地址：https://github.com/JINMMYSC/WEIXIN

已通过 GitHub CLI 验证 JINMMYSC 对该私有仓库有管理员权限。基础工程位于 codex/ios-bootstrap 分支，已推送并触发 Actions；构建状态以实际 CI 运行记录为准。用户已使用手机签名软件安装提交 `3712d42` 的未签名构建，确认键盘扩展可在真实输入框中加载并显示基线候选。

## 已收到的真机证据

### S01-A：重建键盘链路确认（2026-09-12 21:54）

- 环境：iPhone 竖屏、深色外观；具体机型、iOS 版本和显示缩放尚未记录。
- 重建版本：页面文字与提交 `3712d42` 一致。
- 操作：全键盘逐字输入 `nihao`，未选择候选。
- 观察：宿主输入框显示带标记的 `nihao`；键盘组合区显示 `nihao`；候选栏显示 `你好`；主 App、键盘扩展、组合串和候选链路均已在真机成立。
- 限制：基线词表只有一个 `nihao` 候选，因此这条证据不验证分页或原版排序。

### S01-B：原版截图未构成同条件对照

- 原版截图处于九宫格键盘，重建截图处于全键盘。
- 宿主输入框保留了 `nihaonih`，不是从空白状态输入 `nihao`。
- 可见候选依次包含 `你好`、挥手表情、`是`、`你和`、`你会`、`你还`、`你个`，但不能把该顺序归因于单独输入 `nihao`。
- 该截图可作为九宫格界面参考，不进入全键盘 S01 的候选差分基准。
