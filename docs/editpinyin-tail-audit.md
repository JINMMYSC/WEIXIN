# editpinyin 尾部复核

直接读取解包副本得到：文件长度 727244，索引数量 21487。
最后三个索引偏移为 727105、727120、727160。
最后一条记录的 n=4，头部长度 28，累计结束偏移为 [5, 15, 19, 24]。
因此记录正文结束于 727212，至文件末尾剩余 32 字节。

尾部十六进制：
```
2B0000000100000000000000000000000000000000000000000000000000000000
```

全文件按头部长度加最后结束偏移复核，只有最后一个索引区间的长度不相等。
旧报告所称“最后索引指向文件末端、4 字节对齐异常”不正确，应以本次字节复核为准。
尾部含非零值，目前语义未知；不能静默删除或解释为对齐填充。
Swift 完整文件读取仍会拒绝该未解释尾部，不能声称真实文件已成功解码。
记录 payload 含标记字节，本次检查不证明候选文本或排序语义。
